rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for the development sandbox to handle all collections seamlessly
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
